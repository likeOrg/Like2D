# Mouse module
 - [x] make scroll stopping optional
 - [x] mouse setSpeed and setPos for captured mice

# Functional-Style Vec2 helpers
 - [x] Vector2 should take stream iterators into consideration.

# Gamepad extensions
 - [x] Add 'any' to joypad target -- Check all gamepads
 - [x] Add joypad mapping
 - [x] Joypad mapping autosave w/localstorage
 - [x] Add trigger mapping
 - [x] Create a scene for mapping a new joypad, with usage examples
 - [x] Bring back use SDL database default mappings, but with current architecture. Notice commit `ea5b343`.
 - [x] Add back documentation about physical mapping (item above). Notice commits `ea5b343` and `15f2e87`

# Prefab scenes
 - [ ] Move into own folder, such that imports match what a framework consumer would use.
 - [ ] Fix all links to prefab scenes in docs.

# Text bug
 - [ ] Fix text not centering on firefox.

# Actions overhaul
 - [ ] Don't give module deps to Action, have `like:*` events for each thing.
 - [ ] Add `getInputMap` and `setInputMap` for easy access to the entire mapping type object.
 - [ ] Add `like.input.getActionBindingsOf` for a given button

# Logging / crashes
 - [ ] Research better crash handling.
 - [ ] Put a warning ONLY the first time when audio is (or would be) blocked due to autoplay policy.
 - [ ] Put a warning whenever a non-standar gamepad is plugged in without mappings.

# Timer extensions
 - [ ] Consider renaming `Timer` to `Time` and allowing for multiple timers i.e. true scene time.
 - [ ] Wrap settimeout and give users an API for delayed calls + cancelling them.
 - [ ] Allow setting timer speed.

# Documentation
 - [ ] Module documentation such as `Canvas` not appearing on like.canvas `:(`