# explicit null
 - [ ] Every optional argument that causes a deletion / reset should be replaced with an explicit null,
  or split out into a `clear` / `del` function. Nobody should delete their stuff by accident.

# check if has focus
 - [ ] under mouse or under canvas?

# declarative transforms
 - [ ] Every single draw call gets generic transformability -- scale, rotation, etc. in its props table.
 - [ ] props.scale
 - [ ] props.rotate

# transition to github org likeOrg
 - [ ] Change all references here
 - [ ] Fix in npm
 - [ ] fix in jsr
 - [ ] publish new stuff

# Scene stuff
 - [ ] Add like.getScene()
 - [ ] Add like.pushScene() and like.popScene()
 - [ ] like.setScene() -- only sets the top of the stack
 - [ ] Add like.clearSceneStack()
 - [ ] Add use of pushScene / popScene to mapGamepad.ts and document it

# Keyboard Extensions
 - [ ] Track keycodes as well as scancodes
 - [ ] Add justPressed

# Graphics Extensions
 - [ ] Add "withTransform()" to gfx to automate push/pop.
 - [ ] Add "withRenderTarget(canvas, (context2d) => void)" for canvas.

# Action mapping scene
 - [ ] Copy+modify the gamepad mapping scene to make
       it possible to map actions.
 - [ ] When actions fire, they return a gamepad number if relevant.

# Sticks!
 - [ ] Figure out Firefox/Chrome deterministic behavior for d-pads and sticks.
 - [ ] Add stick mapping to mapGamepad scene.
 - [ ] Stick digitization deadzone option.
 - [ ] Stick emulation from buttons.